pipeline{
agent any
 stages {
    stage('Sonar Analyse') {
      environment {
          scannerHome = tool 'SonarQubeScanner'
        }
      steps {
                withSonarQubeEnv('SonarQube') {
                  bat "${scannerHome}/bin/sonar-scanner.bat"
                }
           }
        }
    }
}
