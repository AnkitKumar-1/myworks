pipeline{
 agent any
 environment {
    ANYPOINT = credentials('ANYPOINT')
	  PATH = 'C:/WINDOWS/SYSTEM32'
    sonarqubeScannerHome = tools 'SonarScanner'
 }
 stages {
	stage('SonarQube Analysis'){
    steps {
			withSonarQubeEnv('SonarQube'){
				bat "${sonarqubeScannerHome}/bin/sonar-scanner"
			}
		  }
    }
 }

}
