pipeline {
agent any
stages {

    stage('Sonar Analyse') {
        steps {
                script {
                    withSonarQubeEnv('SonarQube') {
                    def scannerHome = tool 'SonarQubeScanner';
                    bat "${scannerHome}/bin/sonar-scanner"
                }
            }
            }
        }
    }
}


