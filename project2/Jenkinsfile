pipeline{
 agent any
 environment {
    ANYPOINT = credentials('ANYPOINT')
	  PATH = 'C:/WINDOWS/SYSTEM32'
    sonarqubeScannerHome = tool 'SonarScanner'
 }
 stages {
	stage('SonarQube Analysis'){
    steps {
			withSonarQubeEnv('SonarQube'){
				bat "${sonarqubeScannerHome}/sonar-scanner"
			}
		  }
    }
 }

}
