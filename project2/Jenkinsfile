pipeline {
agent any
tools {
    maven 'maven'
}
stages {
    stage('Sonar Analyse') {
      steps {
        script  {
                withSonarQubeEnv('SonarQube') {
                  bat "${JENKINS_HOME}/workspace/angular-pipeline/project2/sonar-scanner.bat"
                }
               }
              } 
            }
        }
    }
